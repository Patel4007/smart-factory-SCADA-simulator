services:
  kafka:
    image: bitnami/kafka:3.7
    ports:
      - "19092:19092"
    environment:
      KAFKA_CFG_NODE_ID: 1
      KAFKA_KRAFT_CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_CFG_PROCESS_ROLES: controller,broker
      KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:19092
      KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,EXTERNAL://localhost:19092
      KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CFG_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE: "true"
      ALLOW_PLAINTEXT_LISTENER: "yes"

  app:
    build: .
    environment:
      SCADA_HOST: 0.0.0.0
      SCADA_PORT: 8000
      SCADA_USE_KAFKA: "true"
      SCADA_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      SCADA_SITE_NAME: North River Plant
      SCADA_LINE_NAME: Line A - Precision Assembly
    depends_on:
      - kafka
    ports:
      - "8000:8000"
